import XCTest
@testable import WWCountdownButton

final class WWCountdownButtonTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(WWCountdownButton().text, "Hello, World!")
    }
}
